version: "3.9"
services:
  k1:
    build: .
    ports:
      - "8080:8080"
    environment:
      K1_API_TOKEN: ${K1_API_TOKEN:-dev-token}
    volumes:
      - k1_data:/app/.k1
    restart: unless-stopped

volumes:
  k1_data:
